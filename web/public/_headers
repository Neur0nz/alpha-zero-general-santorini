# GitHub Pages headers configuration
# Enable cross-origin isolation for SharedArrayBuffer support (required for ONNX runtime)

/*
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Opener-Policy: same-origin

# Cache Python sources aggressively for faster repeat loads
/*.py
  Cache-Control: public, max-age=86400, immutable

# Ensure proper content type and caching for ONNX files
/*.onnx
  Content-Type: application/octet-stream
  Cache-Control: public, max-age=86400, immutable
